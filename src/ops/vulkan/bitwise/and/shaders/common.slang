#ifndef OPENINFER_AND_COMMON_SLANG
#define OPENINFER_AND_COMMON_SLANG 1

struct AndPush {
    uint len;
    uint tensor_count;
    uint params_offset;
    uint flags;
};

#endif // OPENINFER_AND_COMMON_SLANG
